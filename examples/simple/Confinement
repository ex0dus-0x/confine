# Defines basic identifying information
sample:
    name: Simple Command Execution
    image: ./rootfs
    hostname: sample

blacklist:
    logpath: test.log
    rules:
        - syscall: openat
          action: WARN

        - syscall: close
          action: LOG

        - syscall: exit_group
          action: BLOCK

provision:
    setup:
      - name: Build smoke screen test
        trace: false
        command: ["gcc", "example.c"]

    execution:
      - name: Check directory
        command: ["/bin/ls"]

      - name: Run sample
        trace: true
        command: ["./a.out"]
